<nav>
    <div class="nav-wrapper">
        <div class="row">
            <div class="col s12">
                <a href="#" class="brand-logo">Invoices</a>
            </div>
            <ul id="nav-mobile" class="right hide-on-med-and-down">
                {% if user %}
                    <li><a href="{{ urlFor('invoices') }}">My Invoices</a></li>
                    <li><a href="{{ urlFor('login') }}">Sign Out</a></li>
                {% else %}
                    <li><a href="{{ urlFor('login') }}">Login</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>